# Phigros OJ 详细设计说明书

> 版本：v1.0  
> 日期：2026-08-15  
> 作者：EmotionEcho

---

## 1. 引言

### 1.1 编写目的

本文档为 Phigros OJ 项目的详细设计说明书，面向开发者（即作者本人），用于指导系统的编码实现。涵盖系统架构、数据模型、模块设计、接口规范、OCR 流程及反作弊机制等核心设计细节。

### 1.2 术语与缩略语

| 术语 | 含义 |
|------|------|
| RKS | Phigros 中的难度评价指数，用于量化玩家成绩 |
| ACC | Accuracy，准确率，结算界面显示的百分比 |
| ACM 榜单 | 类似 ACM/ICPC 竞赛的矩阵排行榜，横轴题目（歌曲），纵轴选手 |
| 结算截图 | Phigros 一局游戏结束后显示成绩的界面截图 |
| 封榜 | 隐藏最终成绩，在指定时间统一揭晓的机制 |

### 1.3 参考资料

- Phigros OJ README.md
- Phigros Wiki（曲目定数等数据来源）
- PaddleOCR 官方文档

---

## 2. 系统架构设计

### 2.1 总体架构

采用经典的 **B/S 架构**，本地启动 Web 服务，浏览器访问操作。

```
┌─────────────────────────────────────────────────────┐
│                    浏览器 (Frontend)                  │
│  ┌──────────┐  ┌──────────┐  ┌───────────────────┐  │
│  │ 上传页面  │  │ 榜单页面  │  │ 赛事管理页面      │  │
│  └─────┬────┘  └────┬─────┘  └────────┬──────────┘  │
└────────┼────────────┼─────────────────┼──────────────┘
         │ HTTP       │                 │
         ▼            ▼                 ▼
┌─────────────────────────────────────────────────────┐
│                  FastAPI (Backend)                    │
│  ┌──────────┐  ┌──────────┐  ┌───────────────────┐  │
│  │ 认证模块  │  │ 赛事模块  │  │ 成绩提交模块      │  │
│  └──────────┘  └──────────┘  └───────┬───────────┘  │
│  ┌──────────┐  ┌──────────┐          │               │
│  │ 榜单模块  │  │ 导出模块  │          │               │
│  └──────────┘  └──────────┘          │               │
│         │                             │               │
│         ▼                             ▼               │
│  ┌──────────┐  ┌──────────────────────────────────┐  │
│  │ 数据访问  │  │        OCR 识别引擎               │  │
│  │   层     │  │  (PaddleOCR + 图像预处理)         │  │
│  └────┬─────┘  └──────────────────────────────────┘  │
│       │                                              │
│       ▼                                              │
│  ┌──────────┐  ┌──────────────────────────────────┐  │
│  │  SQLite  │  │        反作弊校验模块              │  │
│  └──────────┘  └──────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

### 2.2 技术栈明细

| 层次 | 技术 | 版本要求 | 说明 |
|------|------|----------|------|
| 后端框架 | FastAPI | ≥ 0.100 | 异步高性能，自带 OpenAPI 文档 |
| 前端 | Jinja2 模板 + 原生 JS | - | 轻量，无需构建工具 |
| ORM | SQLAlchemy | ≥ 2.0 | 异步 ORM，类型友好 |
| 数据库 | SQLite | ≥ 3.35 | 本地单文件，支持 JSON 函数 |
| OCR | PaddleOCR | ≥ 2.7 | 高精度中文/数字识别 |
| 图像处理 | Pillow + OpenCV | - | 裁剪、二值化、透视校正 |
| 数据计算 | Pandas | - | 成绩计算、榜单排序 |
| 认证 | JWT (python-jose) | - | 无状态 Token 认证 |
| 密码哈希 | bcrypt (passlib) | - | 安全存储密码 |



---

## 3. 数据模型设计

### 3.1 ER 图

```
┌──────────┐       ┌──────────────┐       ┌──────────┐
│   User   │       │   Contest    │       │   Song   │
├──────────┤       ├──────────────┤       ├──────────┤
│ id       │       │ id           │       │ id       │
│ username │       │ name         │       │ name     │
│ password │       │ description  │       │ artist   │
│ created  │       │ start_time   │       │ bpm      │
└────┬─────┘       │ end_time     │       └────┬─────┘
     │             │ is_sealed    │            │
     │             │ created_by   │───────┐    │
     │             └──────┬───────┘       │    │
     │                    │               │    │
     │             ┌──────┴───────┐       │    │
     │             │ContestSong   │       │    │
     │             ├──────────────┤       │    │
     │             │ contest_id   │───────┘    │
     │             │ song_id      │────────────┘
     │             │ difficulty   │
     │             │ chart_level  │
     │             │ weight       │
     │             └──────┬───────┘
     │                    │
     │             ┌──────┴───────┐
     └────────────►│ Submission   │
                   ├──────────────┤
│ id           │
│ user_id      │
│ contest_id   │
│ contest_song │ (课题曲提交)
│ song_id      │ (自选曲提交)
│ chart_level  │ (自选曲提交)
│ score        │
                   │ accuracy     │
                   │ perfect      │
                   │ good         │
                   │ bad          │
                   │ miss         │
                   │ max_combo    │
                   │ rank         │
                   │ rks          │
                   │ source       │ (ocr / manual)
                   │ image_hash   │
                   │ submitted_at │
                   └──────────────┘
```

### 3.2 表结构详细定义

#### 3.2.1 users 表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTO | 用户 ID |
| username | TEXT | UNIQUE, NOT NULL | 登录用户名 |
| display_name | TEXT | NOT NULL | 显示昵称 |
| password_hash | TEXT | NOT NULL | bcrypt 哈希 |
| is_admin | BOOLEAN | DEFAULT FALSE | 是否为管理员（赛事创建者） |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

#### 3.2.2 songs 表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTO | 歌曲 ID |
| name | TEXT | NOT NULL | 歌曲名称 |
| artist | TEXT | | 曲师 |
| bpm | TEXT | | BPM |
| ez_level | REAL | | EZ 难度定数 |
| hd_level | REAL | | HD 难度定数 |
| in_level | REAL | | IN 难度定数 |
| at_level | REAL | | AT 难度定数（如有） |
| sp_level | REAL | | SP 难度定数（如有） |
| wiki_url | TEXT | | Wiki 链接 |
| created_at | DATETIME | DEFAULT NOW | 录入时间 |

#### 3.2.3 contests 表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTO | 赛事 ID |
| name | TEXT | NOT NULL | 赛事名称 |
| description | TEXT | | 赛事描述 |
| start_time | DATETIME | NOT NULL | 开始时间 |
| end_time | DATETIME | NOT NULL | 截止时间 |
| is_sealed | BOOLEAN | DEFAULT FALSE | 是否封榜 |
| seal_reveal_time | DATETIME | | 揭榜时间 |
| type | TEXT | DEFAULT "topic" | 赛事类型："topic"=课题曲赛事，"free_choice"=自选曲赛事 |
| top_n | INTEGER | | 自选曲赛事阈值 N（每人取前 N 个谱面 RKS），课题曲赛事为空 |
| scoring_rule | TEXT | DEFAULT "rks_sum" | 计分规则标识 |
| created_by | INTEGER | FK → users.id | 创建者 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

#### 3.2.4 contest_songs 表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTO | 关联 ID |
| contest_id | INTEGER | FK → contests.id, NOT NULL | 所属赛事 |
| song_id | INTEGER | FK → songs.id, NOT NULL | 歌曲 |
| difficulty | TEXT | NOT NULL | 难度标识（EZ/HD/IN/AT/SP） |
| chart_level | REAL | NOT NULL | 该谱面定数 |
| weight | REAL | DEFAULT 1.0 | 计分权重倍率 |
| sort_order | INTEGER | DEFAULT 0 | 显示顺序 |
| UNIQUE | (contest_id, song_id, difficulty) | | 同一赛事同一谱面唯一 |

#### 3.2.5 submissions 表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTO | 提交 ID |
| user_id | INTEGER | FK → users.id, NOT NULL | 提交者 |
| contest_id | INTEGER | FK → contests.id, NOT NULL | 所属赛事 |
| contest_song_id | INTEGER | FK → contest_songs.id，课题曲赛事必填 | 赛事谱面（课题曲赛事使用） |
| song_id | INTEGER | FK → songs.id，自选曲赛事必填 | 所选歌曲（自选曲赛事使用） |
| difficulty | TEXT | 自选曲赛事必填 | 所选难度（EZ/HD/IN/AT/SP） |
| chart_level | REAL | 自选曲赛事必填 | 所选谱面定数 |
| score | INTEGER | NOT NULL | 分数（0-1000000） |
| accuracy | REAL | NOT NULL | 准确率（0.0-100.0） |
| perfect | INTEGER | | Perfect 数量 |
| good | INTEGER | | Good 数量 |
| bad | INTEGER | | Bad 数量 |
| miss | INTEGER | | Miss 数量 |
| max_combo | INTEGER | | 最大连击数 |
| rank | TEXT | | 结算评级（Φ/V/S/A/B/C/F） |
| rks | REAL | NOT NULL | 计算得出的 RKS 值 |
| source | TEXT | NOT NULL | 提交来源（"ocr" / "manual"） |
| image_hash | TEXT | UNIQUE | 截图 SHA-256 哈希（OCR 提交时） |
| submitted_at | DATETIME | DEFAULT NOW | 提交时间 |

**关键约束**：
- `image_hash` 字段设为 UNIQUE，确保同一截图不可被重复使用。
- 同一用户对同一赛事谱面可多次提交，榜单取最高 RKS。
- 提交字段与赛事类型匹配：课题曲赛事必须提供 `contest_song_id`；自选曲赛事必须提供 `song_id` + `difficulty`（`chart_level` 由歌曲库或提交数据确定）。

---

## 4. 核心模块详细设计

### 4.1 用户与认证模块

#### 4.1.1 功能描述

- 用户注册：提供用户名、显示昵称、密码。
- 用户登录：用户名 + 密码 → JWT Token。
- 角色区分：普通用户（提交成绩）、管理员（创建/管理赛事）。
- 认证方式：无状态 JWT，Token 存储于浏览器 localStorage。

#### 4.1.2 接口设计

```
POST   /api/auth/register      注册
POST   /api/auth/login         登录 → 返回 JWT
GET    /api/auth/me            获取当前用户信息
```

#### 4.1.3 注册请求/响应

```json
// POST /api/auth/register
// Request
{
    "username": "player1",
    "display_name": "玩家一号",
    "password": "******"
}
// Response 201
{
    "id": 1,
    "username": "player1",
    "display_name": "玩家一号"
}
```

#### 4.1.4 登录响应

```json
// POST /api/auth/login
// Request
{
    "username": "player1",
    "password": "******"
}
// Response 200
{
    "access_token": "eyJ...",
    "token_type": "bearer",
    "user": {
        "id": 1,
        "username": "player1",
        "display_name": "玩家一号",
        "is_admin": false
    }
}
```

---

### 4.2 赛事管理模块

#### 4.2.1 功能描述

- 管理员创建赛事：设定名称、时间范围、赛事类型与课题曲列表。
- 两种赛事类型：**课题曲赛事**（指定曲目作为"题目"）与**自选曲赛事**（选手自由选曲，取每人全部谱面 RKS 的前 N 个进榜）。
- 添加课题曲：从歌曲库中选择，指定难度和可选权重（课题曲赛事）。
- 赛事状态管理：未开始 → 进行中 → 已结束。
- 封榜/揭榜：封榜期间排行榜隐藏具体成绩，仅显示排名。
- **自动揭榜（已实现）**：管理员封榜时可设置 `seal_reveal_time`（揭榜时间）；
  服务端在访问榜单/赛事详情/赛事列表时惰性检查，到点自动解除封榜并清空揭榜时间，
  成绩自动恢复可见。未设置揭榜时间则维持手动揭榜。

#### 4.2.2 接口设计

```
POST   /api/contests                  创建赛事（管理员）
GET    /api/contests                  赛事列表
GET    /api/contests/{id}             赛事详情
PUT    /api/contests/{id}             编辑赛事（管理员）
POST   /api/contests/{id}/songs       添加课题曲（管理员）
DELETE /api/contests/{id}/songs/{sid} 移除课题曲（管理员）
POST   /api/contests/{id}/seal        封榜（管理员）
POST   /api/contests/{id}/reveal      揭榜（管理员）
```

#### 4.2.3 创建赛事请求

```json
// POST /api/contests
{
    "name": "Phigros 小群赛 #1",
    "description": "第一届测试赛",
    "type": "topic",
    "start_time": "2026-09-01T00:00:00",
    "end_time": "2026-09-07T23:59:59",
    "top_n": null,
    "songs": [
        {"song_id": 1, "difficulty": "IN", "weight": 1.0},
        {"song_id": 2, "difficulty": "AT", "weight": 1.5}
    ]
}
```

> 自选曲赛事：`type` 为 `"free_choice"`，设置 `top_n`（如 5），无需提供 `songs`。

---

### 4.3 成绩提交与 OCR 模块

#### 4.3.1 功能描述

- **OCR 自动模式**：上传结算截图 → 图像预处理 → PaddleOCR 识别 → 字段解析 → 数据校验 → 存储。
- **手动录入模式**：用户手动填写 Score、ACC 等字段 → 数据校验 → 存储。
- **去重校验**：计算截图哈希，与已有 `image_hash` 比对，防止同一截图重复使用。
- **字段一致性校验**：检查 Score 与 Note 数的逻辑匹配关系。

#### 4.3.2 接口设计

```
POST   /api/submissions/ocr           OCR 提交（上传截图）
POST   /api/submissions/manual        手动提交
GET    /api/submissions               我的提交记录
GET    /api/submissions?contest_id=1  某赛事的提交记录（管理员）
```

#### 4.3.3 OCR 提交流程

```
用户上传截图
    │
    ▼
计算截图 SHA-256 哈希
    │
    ▼
查询 submissions.image_hash 是否已存在 ──是──► 拒绝：截图已被使用
    │否
    ▼
图像预处理（裁剪、二值化、缩放）
    │
    ▼
按区域裁剪关键字段区域
    │
    ▼
PaddleOCR 逐区域识别
    │
    ▼
解析 OCR 结果 → 结构化数据
    │
    ▼
字段一致性校验
    │
    ├── 校验失败 ──► 返回识别结果，提示用户手动修正
    │
    ▼ 校验通过
计算 RKS
    │
    ▼
存储 submission（含 image_hash）
    │
    ▼
返回提交成功 + RKS 值
```

#### 4.3.4 手动提交请求

```json
// POST /api/submissions/manual
{
    "contest_id": 1,
    "contest_song_id": 1,
    "score": 998000,
    "accuracy": 99.80,
    "perfect": 1200,
    "good": 2,
    "bad": 0,
    "miss": 0,
    "max_combo": 1200,
    "rank": "V"
}
```

> 自选曲赛事手动提交：不传 `contest_song_id`，改传 `song_id` + `difficulty`（可选 `chart_level`）。

---

### 4.4 RKS 计算引擎

#### 4.4.1 计算公式

Phigros 社区公认的 RKS 计算公式：

```
若 ACC < 70%:   RKS = 0
若 ACC ≥ 70%:   RKS = chart_level × ((ACC - 55) / 45) ^ 2
若 ACC = 100%:  RKS = chart_level  (即理论值)
```

其中：
- `ACC` 为准确率，取值 0.0 ~ 100.0
- `chart_level` 为谱面定数（如 15.8）

#### 4.4.2 榜单 RKS 聚合规则

1. **单曲最高**：同一用户对同一赛事谱面（或同一歌曲同一难度）的多次提交，取 RKS 最高的一次。
2. **总分计算（课题曲）**：用户总分 = Σ(各赛事谱面最高 RKS × 该谱面 weight)。
3. **总分计算（自选曲）**：用户总分 = 该赛事内全部谱面 RKS 的前 N 个最大值之和（N 取赛事 `top_n`，未满 N 个按实际数量求和）。
4. **排名规则**：按总分降序排列；总分相同时，按最高单曲 RKS 降序。

#### 4.4.3 核心代码逻辑（伪代码）

```python
def calculate_rks(accuracy: float, chart_level: float) -> float:
    if accuracy < 70.0:
        return 0.0
    if accuracy >= 100.0:
        return chart_level
    return chart_level * ((accuracy - 55.0) / 45.0) ** 2

def get_best_rks(user_id: int, contest_song_id: int) -> float:
    submissions = get_submissions(user_id, contest_song_id)
    return max((s.rks for s in submissions), default=0.0)

def get_total_score(user_id: int, contest_id: int) -> float:
    contest_songs = get_contest_songs(contest_id)
    return sum(
        get_best_rks(user_id, cs.id) * cs.weight
        for cs in contest_songs
    )
```

---

### 4.5 排行榜模块

#### 4.5.1 ACM 风格矩阵榜单设计

```
┌──────────┬──────────┬──────────┬──────────┬──────────┬──────────┐
│  排名     │  选手     │  曲目A   │  曲目B   │  曲目C   │  总分    │
│          │          │  IN 15.8 │  AT 16.2 │  IN 14.5 │          │
├──────────┼──────────┼──────────┼──────────┼──────────┼──────────┤
│    1     │  玩家甲   │  15.60   │  16.10   │  14.50   │  46.20   │
│          │          │  (99.8%) │  (99.5%) │ (100.0%) │          │
├──────────┼──────────┼──────────┼──────────┼──────────┤
│    2     │  玩家乙   │  15.40   │  15.80   │  14.20   │  45.40   │
│          │          │  (99.2%) │  (98.0%) │  (99.5%) │          │
├──────────┼──────────┼──────────┼──────────┼──────────┤
│    3     │  玩家丙   │  14.90   │    --    │  14.00   │  28.90   │
│          │          │  (97.0%) │  未提交   │  (98.0%) │          │
└──────────┴──────────┴──────────┴──────────┴──────────┴──────────┘
```

> 自选曲赛事榜单：无固定题目列，按总分排名，并展开每位选手的前 N 个谱面（曲名 + 难度 + RKS）。

#### 4.5.2 单元格状态与颜色

| 状态 | 条件 | 背景色 | 说明 |
|------|------|--------|------|
| 未提交 | 无有效提交 | 灰色 | -- |
| 已提交 | RKS < chart_level × 0.9 | 白色 | 普通成绩 |
| 优秀 | RKS ≥ chart_level × 0.9 | 浅蓝 | 高水平成绩 |
| 满分 | ACC = 100% | 金色 | 理论值 Φ |

#### 4.5.3 封榜模式

- 封榜期间：排行榜仅显示排名，单元格内容替换为 `?`，总分隐藏。
- 揭榜后：恢复完整显示。
- 管理员始终可见完整榜单。

#### 4.5.4 接口设计

```
GET    /api/leaderboard/{contest_id}        获取榜单数据
GET    /api/leaderboard/{contest_id}/export  导出榜单
```

#### 4.5.5 榜单数据结构

```json
// GET /api/leaderboard/1
{
    "contest": {
        "id": 1,
        "name": "Phigros 小群赛 #1",
        "is_sealed": false
    },
    "songs": [
        {"id": 1, "name": "曲目A", "difficulty": "IN", "chart_level": 15.8, "weight": 1.0},
        {"id": 2, "name": "曲目B", "difficulty": "AT", "chart_level": 16.2, "weight": 1.5}
    ],
    "rankings": [
        {
            "rank": 1,
            "user": {"id": 1, "display_name": "玩家甲"},
            "total_score": 39.90,
            "songs": [
                {"rks": 15.60, "accuracy": 99.8, "rank_badge": "V"},
                {"rks": 16.10, "accuracy": 99.5, "rank_badge": "S"}
            ]
        }
    ]
}
```

---

### 4.6 反作弊模块

#### 4.6.1 截图哈希去重

**目标**：防止同一张截图被不同用户（或同一用户对不同谱面）重复提交。

**方案**：
1. 上传截图时，计算整个图片的 SHA-256 哈希值。
2. 将哈希值存入 `submissions.image_hash` 字段（UNIQUE 约束）。
3. 提交前查询是否已存在相同哈希：
   - 若存在且 `user_id` 相同、`contest_song_id` 相同 → 允许（视为重新提交同一成绩；自选曲赛事按 `song_id` + `difficulty` 判定）。
   - 若存在但 `user_id` 不同，或赛事谱面/所选歌曲不同 → 拒绝，提示“该截图已被使用”。

```python
import hashlib
from PIL import Image
import io

def compute_image_hash(image_bytes: bytes) -> str:
    return hashlib.sha256(image_bytes).hexdigest()

def check_image_duplicate(image_hash: str, user_id: int, contest_song_id: int) -> bool:
    existing = db.query(Submission).filter(Submission.image_hash == image_hash).first()
    if existing is None:
        return False  # 无重复
    if existing.user_id == user_id and existing.contest_song_id == contest_song_id:
        return False  # 同一用户同一谱面，允许
    return True  # 不同用户或不同谱面，拒绝
```

#### 4.6.2 字段一致性校验

**目标**：检查 OCR 识别结果或手动输入数据的逻辑一致性。

**校验规则**：

| 编号 | 校验项 | 规则 | 严重级别 |
|------|--------|------|----------|
| V-01 | Score 范围 | 0 ≤ Score ≤ 1,000,000 | 错误 |
| V-02 | ACC 范围 | 0.0 ≤ ACC ≤ 100.0 | 错误 |
| V-03 | Note 数一致性 | Perfect + Good + Bad + Miss = 总 Note 数（若已知） | 警告 |
| V-04 | ACC 与 Score 关系 | ACC 应与 Score 在合理范围内对应 | 警告 |
| V-05 | Max Combo 范围 | 1 ≤ Max Combo ≤ Perfect + Good | 警告 |
| V-06 | Rank 与 Score 对应 | Φ: Score=1000000, V: Score≥960000, S: Score≥920000, ... | 警告 |

**处理策略**：
- **错误级别**：直接拒绝提交，返回具体校验失败原因。
- **警告级别**：提示用户确认，用户可选择强制提交（附加警告标记）。

#### 4.6.3 感知哈希辅助（可选增强）

为防止截图被轻微修改（如加滤镜、微调像素）后绕过精确哈希检测，可额外计算感知哈希（pHash / dHash）：

```python
import imagehash
from PIL import Image

def compute_perceptual_hash(image: Image.Image) -> str:
    return str(imagehash.phash(image, hash_size=16))

def check_near_duplicate(phash: str, threshold: int = 10) -> list:
    all_hashes = db.query(Submission.perceptual_hash).all()
    return [
        h for h in all_hashes
        if imagehash.hex_to_hash(phash) - imagehash.hex_to_hash(h) < threshold
    ]
```

- 感知哈希汉明距离 < 阈值 → 疑似重复，人工审核或标记。
- Phase 2 可选实现。

---

### 4.7 OCR 识别引擎详细设计

#### 4.7.1 Phigros 结算界面区域定义

Phigros 结算界面布局相对固定，关键字段位置如下（以 1080×1920 为基准）：

```
┌─────────────────────────────────┐
│                                 │
│         [曲绘区域]              │  上半部分
│                                 │
├─────────────────────────────────┤
│   歌曲名称                      │
│   难度标识 (IN/AT/...)          │
│                                 │
│   Score:  998,000               │  ← 区域 A
│   Accuracy: 99.80%              │  ← 区域 B
│                                 │
│   Perfect: 1200                 │  ← 区域 C
│   Good: 2                       │
│   Bad: 0                        │
│   Miss: 0                       │
│                                 │
│   Max Combo: 1200               │  ← 区域 D
│   Rank: V                       │  ← 区域 E
│                                 │
└─────────────────────────────────┘
```

> 实际定位统一使用**屏幕百分比坐标**（见 4.7.2），以兼容主流分辨率与屏幕尺寸，而非固定像素。

#### 4.7.2 区域配置文件格式

```json
// ocr_templates/regions_default.json
{
    "reference_resolution": [1080, 1920],
    "regions": {
        "score":     {"x_pct": 9.3, "y_pct": 49.5, "w_pct": 81.5, "h_pct": 4.2},
        "accuracy":  {"x_pct": 9.3, "y_pct": 54.2, "w_pct": 81.5, "h_pct": 3.1},
        "perfect":   {"x_pct": 9.3, "y_pct": 59.9, "w_pct": 37.0, "h_pct": 2.6},
        "good":      {"x_pct": 9.3, "y_pct": 63.0, "w_pct": 37.0, "h_pct": 2.6},
        "bad":       {"x_pct": 9.3, "y_pct": 66.1, "w_pct": 37.0, "h_pct": 2.6},
        "miss":      {"x_pct": 9.3, "y_pct": 69.3, "w_pct": 37.0, "h_pct": 2.6},
        "max_combo": {"x_pct": 9.3, "y_pct": 74.0, "w_pct": 55.6, "h_pct": 2.6},
        "rank":      {"x_pct": 74.1, "y_pct": 78.1, "w_pct": 18.5, "h_pct": 5.2},
        "song_name": {"x_pct": 4.6, "y_pct": 44.3, "w_pct": 90.7, "h_pct": 3.1},
        "difficulty": {"x_pct": 4.6, "y_pct": 41.7, "w_pct": 18.5, "h_pct": 2.1}
    }
}
```

> **注意**：以上百分比为按 1080×1920 基准换算的示意值，需通过 ./Test_sample 实际截图校准。区域坐标一律以屏幕宽高百分比定义，运行时按实际图片尺寸换算为像素，天然适配主流分辨率；如需按特定分辨率微调，可另建 `regions_{width}x{height}.json` 覆盖默认配置。

#### 4.7.2a 实测布局校准（v1.0，以 ./Test_sample 为基准）

> 当前样本全部为**横屏**结算界面，按宽高比分为三个布局桶，区域配置见
> `ocr_templates/regions_default.json`，实现见 `ocr/settle_extract.py`：
>
> | 布局桶 | 宽高比 | 样本 | 布局特征 |
> |--------|--------|------|----------|
> | widescreen_16_9 | 1.778 | MuMu 1920×1080 | 左侧曲绘，右侧成绩面板 |
> | widescreen_16_10 | 1.6 | MuMu 1920×1200 | 同上，垂直方向整体上移 |
> | phone_2.2 | 2.206 | 手机 2400×1088 | 同上，含状态栏/截图工具栏，正文区域垂直居中 |
>
> 关键结论：
> - **曲名与难度**位于曲绘底部灰度层之上，为白字；曲绘中部可能存在美术文字（如
>   "Liminality"、"Pigeon Games"），且部分样本的曲名在曲绘内重复出现，全图 OCR 会
>   误取美术字。因此对曲名/难度区域先做**高亮度白字二值化**（阈值 180/210，2 倍放大）
>   再识别，可排除曲绘纹理干扰。
> - **成绩面板**（右侧）为 UI 白底文本，直接按区域裁剪识别即可；音符数行小字号数字
>   需按 1x/2x/3x 多倍数识别后择优合并。
> - **曲名词典校对**：OCR 对游戏斜体中文存在稳定的形近字误读（如"星拂云锦"→"陎痳堁踞"），
>   通用识别模型（PP-OCRv6 / v5 server）均无法根除；利用 `Illu_Scrapy/songlist.json`
>   已知曲目库做归一化模糊匹配，可将识别结果对齐为规范曲名（如 `Cipher:/2&//<0` →
>   `Cipher : /2&//<|0`、`百鬼@夜行` → `百鬼֎夜行`、`οσειδν` → `Ποσειδών`）。
> - **评级**：大号字母经 OCR 可能误读（如 Φ→4、S→5），置信度偏低，仅供展示参考。
> - 竖屏 9:16 布局暂无样本，列为后续适配项。

#### 4.7.3 图像预处理流水线

```
原始截图
    │
    ▼
1. 尺寸归一化：将百分比区域换算为像素后裁剪（必要时先缩放至基准分辨率，如 1080×1920）
    │
    ▼
2. 灰度化：转为灰度图，减少颜色干扰
    │
    ▼
3. 二值化：自适应阈值二值化，增强文字对比度
    │
    ▼
4. 区域裁剪：根据配置文件裁剪各字段区域
    │
    ▼
5. 字符级增强：对裁剪区域做去噪、膨胀/腐蚀
    │
    ▼
送入 PaddleOCR 识别
```

#### 4.7.4 OCR 结果解析

PaddleOCR 输出为文本列表，需解析为结构化数据：

```python
import re

def parse_score(text: str) -> int:
    cleaned = re.sub(r'[,\s]', '', text)
    return int(cleaned)

def parse_accuracy(text: str) -> float:
    match = re.search(r'(\d+\.?\d*)%?', text)
    return float(match.group(1)) if match else -1.0

def parse_note_count(text: str) -> int:
    match = re.search(r'(\d+)', text)
    return int(match.group(1)) if match else -1

def parse_rank(text: str) -> str:
    rank_map = {"Φ": "PHI", "V": "V", "S": "S", "A": "A", "B": "B", "C": "C", "F": "F"}
    for char in text.upper():
        if char in rank_map or char == "Φ":
            return rank_map.get(char, char)
    return "UNKNOWN"
```

#### 4.7.5 OCR 准确率保障措施

| 措施 | 说明 |
|------|------|
| 区域裁剪 | 只识别目标区域，减少干扰 |
| 二值化预处理 | 增强数字/文字对比度 |
| 正则后处理 | 清理 OCR 输出中的常见错误（如 O→0, l→1） |
| 字段校验 | 识别后立即做范围检查，异常值标记 |
| 手动修正兜底 | OCR 结果返回前端，用户可修正后提交 |

---

### 4.8 导出模块

#### 4.8.1 导出格式

| 格式 | 实现方式 | 说明 |
|------|----------|------|
| Markdown 表格 | 字符串拼接 | 简单文本，适合群内粘贴 |
| HTML 页面 | Jinja2 模板渲染 | 带样式的完整网页 |
| PNG 图片 | Pillow / html2image | 适合图片分享 |

#### 4.8.2 接口设计

```
GET /api/leaderboard/{contest_id}/export?format=markdown
GET /api/leaderboard/{contest_id}/export?format=html
GET /api/leaderboard/{contest_id}/export?format=image
```

---

## 5. 前端页面设计

### 5.1 页面清单

| 页面 | 路由 | 功能 |
|------|------|------|
| 登录 | `/login` | 用户名密码登录 |
| 注册 | `/register` | 注册新账号 |
| 赛事列表 | `/contests` | 浏览所有赛事 |
| 赛事详情 | `/contests/{id}` | 赛事信息 + 课题曲列表 + 榜单入口 |
| 成绩提交 | `/contests/{id}/submit` | 上传截图 / 手动录入 |
| 排行榜 | `/contests/{id}/leaderboard` | ACM 风格矩阵榜单 |

### 5.2 ACM 榜单页面布局

```
┌─────────────────────────────────────────────────────────┐
│  Phigros OJ   [赛事名称]              [登录用户] [退出]  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐    │
│  │  排名 │ 选手 │ 曲A(IN) │ 曲B(AT) │ ... │ 总分  │    │
│  │  ──── │ ──── │ ────── │ ────── │ ... │ ────  │    │
│  │   1   │ 甲   │ 15.60  │ 16.10  │ ... │ 46.20 │    │
│  │   2   │ 乙   │ 15.40  │ 15.80  │ ... │ 45.40 │    │
│  │   3   │ 丙   │ 14.90  │   --   │ ... │ 28.90 │    │
│  └─────────────────────────────────────────────────┘    │
│                                                         │
│  [导出 Markdown] [导出 HTML] [导出图片]                   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 5.3 成绩提交页面布局

```
┌─────────────────────────────────────────────────────────┐
│  Phigros OJ   [赛事名称] - 提交成绩                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  选择课题曲：[▼ 下拉选择 ]                                │
│                                                         │
│  ┌─────────────────────┐  ┌─────────────────────────┐   │
│  │   OCR 自动识别       │  │   手动录入               │   │
│  │                     │  │                         │   │
│  │  [选择截图] 或拖拽   │  │  Score:    [________]   │   │
│  │                     │  │  Accuracy: [________]   │   │
│  │  ┌───────────────┐  │  │  Perfect:  [________]   │   │
│  │  │               │  │  │  Good:     [________]   │   │
│  │  │   预览区域     │  │  │  Bad:      [________]   │   │
│  │  │               │  │  │  Miss:     [________]   │   │
│  │  └───────────────┘  │  │  Max Combo:[________]   │   │
│  │                     │  │  Rank:     [________]   │   │
│  │  [上传识别]          │  │                         │   │
│  │                     │  │  [提交]                  │   │
│  └─────────────────────┘  └─────────────────────────┘   │
│                                                         │
│  识别结果（OCR 模式）：                                   │
│  Score: 998000  ACC: 99.80%  RKS: 15.60                │
│  [确认提交] [修正后提交]                                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 6. 关键流程时序

### 6.1 OCR 成绩提交流程

```
用户            前端              后端API           OCR引擎         反作弊         数据库
 │               │                 │                 │               │              │
 │──选择截图───►│                 │                 │               │              │
 │               │──上传图片──────►│                 │               │              │
 │               │                 │──计算SHA256──►│               │              │
 │               │                 │                 │               │──查重──────►│
 │               │                 │                 │               │◄──无重复────│
 │               │                 │──预处理+裁剪──►│               │              │
 │               │                 │                 │──OCR识别───►│              │
 │               │                 │◄──识别结果─────│               │              │
 │               │                 │──字段校验─────►│               │              │
 │               │                 │──计算RKS──────►│               │              │
 │               │                 │──存储──────────────────────────────────────►│
 │               │◄──返回结果─────│                 │               │              │
 │◄──显示RKS────│                 │                 │               │              │
```

### 6.2 排行榜查询流程

```
用户            前端              后端API           数据库
 │               │                 │                 │
 │──访问榜单───►│                 │                 │
 │               │──请求榜单──────►│                 │
 │               │                 │──查询数据──────►│
 │               │                 │◄──原始数据──────│
 │               │                 │                 │
 │               │                 │──聚合计算:      │
 │               │                 │  ·每人每曲最高RKS│
 │               │                 │  ·加权总分       │
 │               │                 │  ·排序           │
 │               │                 │  ·封榜处理       │
 │               │◄──榜单数据─────│                 │
 │◄──渲染矩阵───│                 │                 │
```

---

## 7. 配置管理

### 7.1 应用配置文件

```yaml
# config.yaml
app:
  name: "Phigros OJ"
  host: "127.0.0.1"
  port: 8000
  debug: false
  secret_key: "CHANGE_ME_IN_PRODUCTION"

database:
  url: "sqlite:///data/phigros_oj.db"

ocr:
  engine: "paddleocr"
  template_dir: "ocr_templates"
  default_resolution: [1080, 1920]
  languages: ["ch", "en"]

anti_cheat:
  enable_hash_dedup: true
  enable_perceptual_hash: false
  perceptual_hash_threshold: 10
  enable_field_validation: true

jwt:
  algorithm: "HS256"
  access_token_expire_minutes: 1440
```

### 7.2 OCR 区域配置

区域坐标一律以屏幕宽高百分比定义，默认配置存放在 `ocr_templates/regions_default.json`，运行时按实际图片尺寸换算为像素，天然适配主流分辨率与屏幕尺寸。基准布局以 ./Test_sample 目录截图校准；如需按特定分辨率微调，可另建 `regions_{width}x{height}.json` 覆盖。

---

## 8. 部署方案

### 8.1 本地开发运行

```bash
# 安装依赖
pip install -r requirements.txt

# 初始化数据库
python -m app.database init

# 启动服务
python -m app.main
# 或
uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

> **MVP 现状（v1.0）**：`app/` 目录已提供可运行的网页与服务器实现（FastAPI + SQLite +
> Jinja2），启动命令为 `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`。
> 启动时自动建表并导入歌曲库；OCR 集成位于 `app/main.py`（调用 `ocr/analyzer`），
> 排行榜聚合位于 `app/leaderboard.py`。详见 `app/README.md`。

### 8.2 打包分发（Phase 3）

```bash
# PyInstaller 打包
pyinstaller --onefile --name phigros-oj --add-data "ocr_templates;ocr_templates" --add-data "app/templates;app/templates" --add-data "app/static;app/static" run.py
```

生成单一可执行文件，双击即可启动 Web 服务并自动打开浏览器。

---

## 9. 测试计划

### 9.1 单元测试

| 模块 | 测试项 | 预期 |
|------|--------|------|
| RKS 计算 | ACC=100%, level=16.0 | RKS=16.0 |
| RKS 计算 | ACC=99.0%, level=15.8 | RKS≈15.11 |
| RKS 计算 | ACC=69.9%, level=16.0 | RKS=0.0 |
| RKS 计算 | ACC=70.0%, level=16.0 | RKS≈1.78 |
| 字段校验 | Score=1000001 | 校验失败 |
| 字段校验 | ACC=101.0 | 校验失败 |
| 哈希去重 | 相同图片不同用户 | 拒绝 |
| 哈希去重 | 相同图片相同用户相同谱面 | 允许 |

### 9.2 OCR 准确率测试

- 收集 ≥ 50 张不同来源（模拟器/手机/不同分辨率）的结算截图。
- 逐字段统计识别准确率。
- 目标：数字字段（Score、Note 数）准确率 ≥ 99%，ACC 准确率 ≥ 99%（1 位数字精度）。

### 9.3 集成测试

- 完整流程：注册 → 创建赛事 → 上传截图 → 识别 → 榜单更新 → 导出。
- 多用户并发提交（模拟 20-30 人规模）。
- 封榜/揭榜流程验证。

---

## 10. 后续迭代方向

| 方向 | 说明 | 优先级 |
|------|------|--------|
| Discord/QQ Bot 集成 | 群内 `/submit` 命令提交 | 中 |
| 感知哈希反作弊 | 检测轻微修改的截图 | 中 |
| 更多分辨率样本校准 | 扩充 Test_sample 样本库，覆盖主流分辨率 | 高 |
| 曲绘资源抓取（Illu_Scrapy） | 从 Wiki 提取歌曲曲绘文件，供页面展示 | 低（后续考虑） |
| Docker 容器化部署 | 方便服务器部署 | 低 |
| 深色主题 UI | 前端主题切换 | 低 |
